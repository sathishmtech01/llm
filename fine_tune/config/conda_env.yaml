name: llm-finetune-env
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - azureml-sdk
      - torch
      - transformers
      - datasets
      - accelerate
      - peft
      - bitsandbytes
      - trl
